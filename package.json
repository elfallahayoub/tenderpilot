{
  "name": "tenderpilot",
  "version": "0.1.0",
  "private": true,
  "description": "Analyse d'avis d'appel d'offres par une equipe d'agents",
  "scripts": {
    "up": "docker compose up -d --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "reset": "docker compose down -v && docker compose up -d --build && echo. && echo Base remise a zero, schema rejoue, services redemarres. && echo Interface : http://localhost:5173 && echo Sante   : http://localhost:3000/health"
  }
}
